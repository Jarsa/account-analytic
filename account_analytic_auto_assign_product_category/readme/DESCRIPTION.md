Extends `account_analytic_auto_assign` so its rules can also match by
**product category**, driven by a required analytic account field directly
on the product category, instead of a rule created by hand.
