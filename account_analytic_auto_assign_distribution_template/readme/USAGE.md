On any journal item line (e.g. a vendor bill expense line), pick a
**Distribution Template** (enable the column if hidden) — its saved
percentage split is copied onto the line's analytic distribution, ready
to adjust further if needed.
